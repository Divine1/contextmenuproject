import { MergeindicatorDirective } from './mergeindicator.directive';

describe('MergeindicatorDirective', () => {
  it('should create an instance', () => {
    const directive = new MergeindicatorDirective();
    expect(directive).toBeTruthy();
  });
});
